cryptography>=43.0.0
# Bibliotheque certifiee pour AES-256-GCM, Argon2id, CSPRNG
# Version minimale : 43.0.0 (support Argon2id via hazmat primitives)
# Dependance unique — surface d'attaque minimale (principe de moindre privilege)